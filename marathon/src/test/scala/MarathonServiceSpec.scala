package sbtmarathon

import org.scalatest.FlatSpec

class MarathonServiceSpec extends FlatSpec {

  behavior of "MarathonService"

  ignore should "build URLs that target an application instance" in {
  }

  ignore should "build URLs that reference GUI of an application instance" in {
  }

  ignore should "return a Success result when a 20x response is received" in {
  }

  ignore should "return a UserError result when a 40x response is received" in {
  }

  ignore should "return a SystemError result when a 50x response is received" in {
  }
}
